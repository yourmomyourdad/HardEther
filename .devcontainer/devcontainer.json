{
  "name": "HardEther Node Server",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",

  "postCreateCommand": "npm install @roamhq/wrtc && sudo apt-get update && sudo apt-get install -y ffmpeg xdotool xvfb unclutter chromium && npm install && sudo curl -L --output /usr/local/bin/cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 && sudo chmod +x /usr/local/bin/cloudflared",

  "forwardPorts": [8080],

  "postStartCommand": "bash Startup"
}
