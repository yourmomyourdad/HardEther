{
  "name": "HardEther Node Server",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:20",

  "postCreateCommand": "npm install @roamhq/wrtc && sudo apt-get update && sudo apt-get install -y ffmpeg && sudo apt-get install -y xdotool && sudo apt-get install -y xvfb && npm install && npx playwright install --with-deps chromium && sudo curl -L --output /usr/local/bin/cloudflared https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 && sudo apt-get install -y unclutter && sudo chmod +x /usr/local/bin/cloudflared",

  "forwardPorts": [8080],

  "postStartCommand": "bash Startup"
}
